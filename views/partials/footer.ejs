<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
  integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>
<!--<script>
          setInterval( () => {window.location.reload()},5000);
        </script>-->
<script>
document.addEventListener("DOMContentLoaded", function () {

  let initialLoad = true;
  const bars = document.querySelectorAll(".progress-bar");

  // Function that updates the bar width
  function updateProgress() {
    const now = Date.now();

    bars.forEach(bar => {
      const startedAt = parseInt(bar.dataset.startedAt);
      const craftTime = parseInt(bar.dataset.craftTime) * 1000;

      let progress = (now - startedAt) / craftTime;
      if (progress < 0) progress = 0;
      if (progress > 1) progress = 1;

      const li = bar.closest("li");
      const progressContainer = li.querySelector(".progress");
      const completeContainer = li.querySelector(".complete-container");

      bar.style.width = (progress * 100) + "%";

      if (progress >= 1) {

        // If first load, replace progress bar immediately
        if (initialLoad) {
          if (progressContainer && completeContainer) {
            progressContainer.remove();
            completeContainer.style.display = "block";
          }
          return;
        }

        // On subsequent updates, let transition handle it
        return;
      }
    });

    initialLoad = false;
  }

  // Run immediately
  updateProgress();

  // Update every 100 milliseconds
  setInterval(updateProgress, 100);

  // Add transition listener ONCE per bar
  bars.forEach(bar => {
    bar.addEventListener("transitionend", function(event) {
      if (event.propertyName !== "width") return;

      const width = parseFloat(bar.style.width);
      if (width >= 100) {
        const li = bar.closest("li");
        const progressContainer = li.querySelector(".progress");
        const completeContainer = li.querySelector(".complete-container");

        if (progressContainer && completeContainer) {
          progressContainer.remove();
          completeContainer.style.display = "block";
        }
      }
    });
  });

  // Remove completed task from DOM after button click
  const completeForms = document.querySelectorAll(".complete-container form");
  completeForms.forEach(form => {
    form.addEventListener("submit", e => {
      const li = form.closest("li");
      setTimeout(() => li.remove(), 50); // small delay to allow form submission
    });
  });

});
</script>
<div class="container fixed-bottom">
  <footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top">
    <p class="col-md-4 mb-0 text-body-secondary">
      Copyright © <%= new Date().getFullYear() %>
    </p>
  </footer>
</div>
</main>
</div>
</body>

</html>