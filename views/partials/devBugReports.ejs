<div class="card mb-4">
    <div class="card-header">
        <h5 class="mb-0">Open Bug Reports</h5>
    </div>
    <div class="card-body table-responsive">
        <% if (bugs.length===0) { %>
            <p>No bug reports submitted.</p>
        <% } else { %>
            <table class="table table-sm table-striped table-hover table-bordered">
                <thead>
                    <tr>
                        <th>Title</th>
                        <th>Player</th>
                        <th>Status</th>
                        <th>Priority</th>
                        <th>Reported</th>
                        <th>Action</th>
                    </tr>
                </thead>
            
                <tbody>
                    <% activeBugs.forEach(bug => { %>
                        <tr>
                            <td>
                                <%= bug.title %>
                            </td>
                            <td>
                                <% if (bug.player_id) { %>
                                    <a href="/dev?player=<%= bug.player_id %>">
                                        <%= bug.player_name %>
                                    </a>
                                <% } else { %>
                                    Unknown
                                <% } %>
                            </td>
                            <td>
                                <label
                                    class="visually-hidden"
                                    for="bug-status-<%= bug.id %>"
                                >
                                    Status for <%= bug.title %>
                                </label>
                                <select
                                    id="bug-status-<%= bug.id %>"
                                    name="status"
                                    form="bug-form-<%= bug.id %>"
                                    class="form-select form-select-sm"
                                >
                                    <option value="open" <%= bug.status === "open" ? "selected" : "" %>>
                                        Open
                                    </option>
                                    <option value="in_progress" <%= bug.status === "in_progress" ? "selected" : "" %>>
                                        In Progress
                                    </option>
                                    <option value="resolved" <%= bug.status === "resolved" ? "selected" : "" %>>
                                        Resolved
                                    </option>
                                    <option value="wont_fix" <%= bug.status === "wont_fix" ? "selected" : "" %>>
                                        Won't Fix
                                    </option>
                                </select>
                            </td>
                            <td>
                                <label
                                    class="visually-hidden"
                                    for="bug-priority-<%= bug.id %>"
                                >
                                    Priority for <%= bug.title %>
                                </label>
                                <select
                                    id="bug-priority-<%= bug.id %>"
                                    name="priority"
                                    form="bug-form-<%= bug.id %>"
                                    class="form-select form-select-sm"
                                >
                                    <option value="low" <%= bug.priority === "low" ? "selected" : "" %>>
                                        Low
                                    </option>
                                    <option value="medium" <%= bug.priority === "medium" ? "selected" : "" %>>
                                        Medium
                                    </option>
                                    <option value="high" <%= bug.priority === "high" ? "selected" : "" %>>
                                        High
                                    </option> 
                                    <option value="critical" <%= bug.priority === "critical" ? "selected" : "" %>>
                                        Critical
                                    </option>
                                </select>
                            </td>
                            <td>
                                <%= new Date(bug.created_at).toLocaleString("en-GB") %>
                            </td>
                            <td>
                                <form
                                    id="bug-form-<%= bug.id %>"
                                    method="POST"
                                    action="/dev/bugs/<%= bug.id %>"
                                >
                                    <button
                                        type="submit"
                                        class="btn btn-sm btn-primary"
                                    >
                                        Update
                                    </button>
                                </form>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } %>
    </div>
</div>
<div class="card mb-4">
    <div class="card-header">
        <h5 class="mb-0">Completed Bugs</h5>
    </div>
    <div class="card-body table-responsive">
        <% if (completedBugs.length === 0) { %>
            <p>No completed bugs.</p>
        <% } else { %>
            <table class="table table-sm table-striped table-hover table-bordered">
                <thead>
                    <tr>
                        <th>Title</th>
                        <th>Player</th>
                        <th>Status</th>
                        <th>Priority</th>
                        <th>Reported</th>
                        <th>Action</th>
                    </tr>
                </thead>

                <tbody>
                    <% completedBugs.forEach(bug => { %>
                        <tr>
                            <td>
                                <%= bug.title %>
                            </td>
                            <td>
                                <% if (bug.player_id) { %>
                                    <a href="/dev?player=<%= bug.player_id %>">
                                        <%= bug.player_name %>
                                    </a>
                                <% } else { %>
                                    Unknown
                                <% } %>
                            </td>
                            <td>
                                <label
                                    class="visually-hidden"
                                    for="completed-bug-status-<%= bug.id %>"
                                >
                                    Status for <%= bug.title %>
                                </label>
                                <select
                                    id="completed-bug-status-<%= bug.id %>"
                                    name="status"
                                    form="completed-bug-form-<%= bug.id %>"
                                    class="form-select form-select-sm"
                                >
                                    <option value="open" <%= bug.status === "open" ? "selected" : "" %>>
                                        Open
                                    </option>
                                    <option value="in_progress" <%= bug.status === "in_progress" ? "selected" : "" %>>
                                        In Progress
                                    </option>
                                    <option value="resolved" <%= bug.status === "resolved" ? "selected" : "" %>>
                                        Resolved
                                    </option>
                                    <option value="wont_fix" <%= bug.status === "wont_fix" ? "selected" : "" %>>
                                        Won't Fix
                                    </option>
                                </select>
                            </td>
                            <td>
                                <label
                                    class="visually-hidden"
                                    for="completed-bug-priority-<%= bug.id %>"
                                >
                                    Priority for <%= bug.title %>
                                </label>
                                <select
                                    id="completed-bug-priority-<%= bug.id %>"
                                    name="priority"
                                    form="completed-bug-form-<%= bug.id %>"
                                    class="form-select form-select-sm"
                                >
                                    <option value="low" <%= bug.priority === "low" ? "selected" : "" %>>
                                        Low
                                    </option>
                                    <option value="medium" <%= bug.priority === "medium" ? "selected" : "" %>>
                                        Medium
                                    </option>
                                    <option value="high" <%= bug.priority === "high" ? "selected" : "" %>>
                                        High
                                    </option>
                                    <option value="critical" <%= bug.priority === "critical" ? "selected" : "" %>>
                                        Critical
                                    </option>
                                </select>
                            </td>
                            <td>
                                <%= new Date(bug.created_at).toLocaleString("en-GB") %>
                            </td>
                            <td>
                                <form
                                    id="completed-bug-form-<%= bug.id %>"
                                    method="POST"
                                    action="/dev/bugs/<%= bug.id %>"
                                >
                                    <button
                                        type="submit"
                                        class="btn btn-sm btn-primary"
                                    >
                                        Update
                                    </button>
                                </form>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } %>
    </div>
</div>